# C# Learning Progress

## Current Position
- **Stage:** A — C# Language Mastery
- **Phase:** 1 — Core Language Foundations — ✅ **COMPLETE** (incl. capstone)
- **Phase:** 2 — Object-Oriented C# — ✅ **COMPLETE (2026-07-27)**. All ten concepts taught, capstone (`Phase2.FinalTask`, Parking Lot) built & verified end-to-end, quiz-debt cleared.
- **Phase:** 3 — The Power Tools — ✅ **COMPLETE (2026-07-28)**. All topics + CSV capstone + events done & verified.
- **Phase:** 4 — Advanced C# — **IN PROGRESS (started 2026-08-02).** `Phase4.Advanced` project created & added to `CSharpMastery.slnx`. **Exceptions done right — COMPLETE**. **`IDisposable`/`using` — COMPLETE (2026-08-04)**. **async/await core — COMPLETE (2026-08-04)**. **Concurrency — race + `lock` + `Interlocked` COMPLETE (2026-08-04)**, lock pitfalls & concurrent collections delivered as rules (no task). **▶ NEXT: memory & the GC**, then reflection/attributes, modern C# features tour, **structs (still owed — only ever mentioned)**, read real OSS code → **Stage A capstone + language exam** (gate to Stage B).
- **Quiz debt — CLEARED 2026-08-04, but by DELIVERY not by ANSWER.** He said "wrap the quiz and move on" without attempting Q1/Q2/Q3, so I gave the written answers instead (per the 4th-strike written-reteach rule): (Q1) virtual-on-base makes the type system *lie* — `cash.Refund(50m)` compiles and silently returns `0m`; moved to `IRefundable` the same line is **CS1061 at compile time**; memorable form = *"a virtual base member makes the type system lie; an interface makes the compiler catch it."* (Q2) `new` hides → **declared/compile-time type decides** → prints `Base`. (Q3) duplicate `catch (HttpRequestException)` with the filtered one second = unreachable → **CS0160, a compile ERROR** (he built it and reported the exact code — good error-reading). **⚠️ Q1 has now been asked 4× and never once answered by him.** Next session: ask it cold, first thing, before any new material. If he can't produce it after being handed the written answer, treat it as a hard blocker for the Stage-A exam, not a footnote.
- **Old quiz debt (2026-08-02, mid-Phase-4):** Q1 (lost-update race) ✅ landed cleanly this time — first fully correct mechanism, casing READ/MODIFY/WRITE explicitly. Q2 (`new`/hiding = declared type decides) ❌ **2nd miss** — said derived (2) prints through a base-typed variable; rebuilt from scratch, correctly restated both halves (new=compile-time/declared-type, override=runtime/actual-type) after guided questions. Q3 (`virtual` base method "lies" vs `IRefundable` compile error) ❌ **3rd time this exact question has come back unanswered or restated instead of answered** (2026-07-19, 2026-07-26, 2026-08-02) — had to be walked step-by-step through the concrete `CashPayment` scenario before landing; asked me to just explain it at the end rather than finish deriving it himself. **This is now a confirmed persistent gap, not a one-off — re-quiz again next session, and if it misses a 4th time, stop and do a from-scratch reteach with a written summary, not just spoken.** Q4 (catch-filter ordering) — never asked, still owed. Q5 (empty catch-all worse than crash) — first answer vague ("further error"), corrected to the real mechanism (loud+debuggable vs silent+untraceable failure).
- **Topic (Phase 3, all done):** Covered → **Generics** (generic class `Box<T>`, generic method `MathUtils.Max<T>` + `where T : IComparable<T>`, inference, `default!`), **Collections choice** (List/Dictionary/HashSet by access pattern; `CountWords` → Dictionary + `GetValueOrDefault`), **Iterators** (`yield return`, deferred execution, multiple-enumeration trap → `.ToList()`), **Delegates/lambdas** (`Action`/`Func`/`Predicate`, higher-order `ApplyToEach`, **closure captured-variable trap** — `for` shares one `i`→`3 3 3`, `foreach` is per-iteration; fix = copy to local), **LINQ deep** (`Where`/`Select`/`OrderBy(Descending)`/`First`-`FirstOrDefault`-`Single` family/`GroupBy`+aggregation, anonymous-type projections, method syntax), **Extension methods** (built his own `MyWhere<T>` = generics+extension+delegate+yield → proved LINQ's laziness is just `yield`), **Tuples & deconstruction** (named tuple `GetStats` → `var (count, sum, average) = ...`).
- **▶ RESUME HERE (next session):** **Phase 3 CAPSTONE — in-memory CSV analysis tool** (load a CSV, query it with LINQ every way). Only remaining Phase-3 item. Then Phase 3 done → **Phase 4** (exceptions, async/await, IDisposable, GC, reflection, modern C#). *(Deferred/optional: `events` and `IEnumerable` vs `IQueryable` conceptual preview were not drilled — touch in capstone or defer to Stage B EF Core. Structs still only mentioned — cover in Phase 4.)* **Carry-over drill:** re-quiz the 3 shaky quiz mechanisms (lost-update race, `new`/hiding = declared type decides, interface-vs-virtual = compile vs runtime error).

### 🅿️ Capstone status — `Phase2.FinalTask` (Parking Lot)
**Builds clean (0 warnings). Does NOT run yet** (`Program.cs` is an empty infinite loop). Design was sketched on paper first (good), then reviewed over several rounds.

**✅ Done & correct — the vehicle layer:**
- `Vehicle` abstract class: `LicenseNo`/`Model`/`Color` (validated setters), plus **`abstract decimal HourlyRate`** and **`abstract VehicleSize RequiredSize`** — real polymorphism after an initial wrong turn (had delegated rate to an external `switch`-on-size class + a `GetVehicleType()` size-switch; both correctly deleted after the type-branching anti-pattern was explained, with the Open/Closed payoff shown live).
- `Motorcycle`/`Car`/`Truck` each `override` both members with literal values. **⚠️ CHECK: `Car.RequiredSize` was `Small` (copy-paste bug, should be `Medium`) — flagged, unconfirmed whether fixed.**
- `VehicleSize` enum (Small/Medium/Large — order matters, enables `>=` size comparison).

**❌ Not done — the orchestration layer (the resume work):**
1. **`Spots.cs` → rename to `Space`, make it a `class` not a `record`** (it's an entity with identity `SpotId` + **mutable** `IsOccupied` → class). Give it `private set` on `IsOccupied` and **`Occupy()`/`Free()` methods** that throw if the invariant is violated. Currently a `record` with public setters and no methods — the encapsulation centrepiece is missing. *(He kept re-submitting it as a record 3× — revisit the entity-vs-value-object reasoning here specifically.)*
2. **`ParkingLot` must COMPOSE the spaces** — hold a `List<Space>`/array, and `Park(Vehicle)` must **find the first free space whose `Size >= vehicle.RequiredSize`**, occupy it, issue a ticket; throw if none fits. Right now it ignores spots entirely (keyed only by license), so 3 of 4 invariants (double-occupancy, size-fit, find-a-spot) are **unenforced**. Change signature from `ParkVehicle(vehicle, spotId string)` → `Ticket Park(Vehicle)` (the lot picks the spot, not the caller).
3. **Remove the redundant `_vehicles` dictionary** (`_parkedVehicles` already holds the vehicle via `ParkingData.Vehicle`).
4. **`ParkingData` (the ticket):** rename → `Ticket`; make it a **class** with `DateTime? ExitTime` instead of the `DateTime.MinValue` sentinel (the sentinel is why `CalculateParkingFee` needs a `Math.Max(fee,0)` band-aid). Uses a `Guid` for identity — fine, but the exercise wanted a **`static` counter** to exercise static state; either is acceptable, note the trade-off.
5. **`InitializeSpots`** — a class holding one static seed method; fold the seed data onto/into the `ParkingLot` (a lot is created *with* its spaces).
6. **`Program.cs` — replace the `while(runCli){}` infinite empty loop** with a real demo: build the lot, park a motorcycle/car/truck, print tickets, exit one, print the fee. **Then run it and observe output** — the capstone isn't done until it runs.

> ✅ **ENVIRONMENT RESOLVED (2026-07-20).** Earlier in the session every build failed with `NETSDK1226` / `MSB4018`. Diagnosis: the snap was **mid-refresh** — SDK `10.0.109` / runtime `10.0.9` on disk with `packs/Microsoft.NETCore.App.Ref/10.0.9` **empty (0 files)**, so there were no reference assemblies to compile against. It was never a code problem. The snap finished refreshing during the session (SDK `10.0.110`, runtime `10.0.10`, ref pack now 348 files) and **`dotnet build` succeeds with 0 warnings / 0 errors.** Lesson worth keeping: *a build error naming SDK paths rather than your source is an environment fault — read the error, don't edit the code.*
>
> Note: the mentor's sandboxed shell cannot **launch** the snap-confined runtime (`dotnet run` aborts there), so **program output must be produced by running it locally and pasted in.** Building/reviewing works fine.

---

## Completed Topics
- 2026-07-10 — Phase 0: SDK vs Runtime, `dotnet` CLI (new/run/build/watch), IL/CLR/JIT mental model, `.sln`/`.slnx` vs `.csproj` read line-by-line
- 2026-07-11 — Phase 1: Program structure (top-level statements vs `Main`), file-scoped namespaces, `using`, one-compilation-unit-per-entry-point rule (CS8802), assembly vs repository/InternalsVisibleTo, access modifiers (`public`/`internal`), static vs instance design decision, C# naming conventions (camelCase/_camelCase/PascalCase)
- 2026-07-11 — Phase 1: Value types vs reference types, stack vs heap, mutate-through-reference vs reassign-the-local-pointer distinction, "pass by value" applied to reference types, boxing/unboxing (concept only)
- 2026-07-11 — Phase 1: Built-in types (int/long/double/decimal/bool/char), `int` vs `System.Int32`, `var` as compile-time inference (not dynamic typing), decimal vs double for money, implicit vs explicit conversion, `checked`/`unchecked` and silent integer overflow, never ignore compiler warnings
- 2026-07-11 — Phase 1: String immutability (methods return new strings, don't mutate), `+=` concat vs `StringBuilder` perf (measured: ~37ms vs 0ms over 10k iterations), string `==` is value equality (overloaded) vs `ReferenceEquals`
- 2026-07-11 — Phase 1: Nullability — `int?`/`Nullable<T>`, nullable reference types (`string?`), compiler static-analysis flow tracking, live `CS8602` warning read and fixed properly (not suppressed with `!`), `??` and `.HasValue`, PascalCase applies to local functions too
- 2026-07-14 — Phase 1: **Methods** — pass-by-value as the default (value type copies data, reference type copies the reference → "the reference is passed by value", NOT "pass by reference"); `ref` (true two-way alias, keyword required at both declaration and call site), `out` (`TryParse` idiom, must assign on every path, out-var leaks to enclosing scope), `in` (read-only ref, perf for large structs). Optional params (compile-time-constant defaults, baked into caller → cross-assembly staleness trap), named args (skip middle optionals, kill boolean-literal mystery at call sites), overloading (differ by params, can't overload on return type). Design judgment: `bool`+`out` is lossy — collapses all failure reasons to one bit; use result type / exception when failure carries info. Built `TryDivide`, `Swap`, `Send` (optional/named), `Describe` overloads in `Describer.cs`.
- 2026-07-16 — Phase 1: **Arrays** — fixed-size + reference type (even `int[]`), 3 initializer forms, zero/`null` default-init trap (`new string[3]` = 3 nulls → NRE), `.Length` is a property, bounds checking (`IndexOutOfRangeException`, last valid index = `Length-1`), integer-division trap avoided via `(double)sum / count` cast placement. `Span<T>` seed only ("zero-copy view over contiguous memory"). Jagged `[][]` vs rectangular `[,]` recognition. 
- 2026-07-16 — Phase 1: **CAPSTONE — Expense Tracker CLI** (`Phase1.CliApplication`). Menu loop + `switch` statement, parallel `decimal[]`/`string[]` + `int count`, `decimal.TryParse`, capacity guard, `:F2` money formatting, all input paths guarded. Built over 3 build rounds. Verified end-to-end with scripted stdin. ✅ Phase 1 complete.
- 2026-07-12 — Phase 1: Operators & control flow — `&&`/`||` short-circuit vs `&`/`|` (correctness tool, null-guard pattern), ternary as an expression. **`switch` expressions** (modern style): value-before-`switch`, `pattern => result` arms, relational patterns (`<`, `<=`), `and`/`or`/`not` combinators, discard `_`, first-match-wins ordering, exhaustiveness. Expression-bodied methods (`=>`). Built `DescribeTemperature` switch-expression through 3 PR rounds.
- 2026-07-19 — Phase 2: **Classes, encapsulation & properties** — why properties exist (public field = unprotectable invariant; C# rejects Java-style get/set ceremony). Three flavours: auto-property, `init`-only, computed/expression-bodied. Hand-written backing field + validation in the accessor; `value` as the implicit setter parameter. **Key idiom: route the constructor through the setters** so construction can't bypass validation. Property vs method decision (`IsExpensive` = adjective/cheap/side-effect-free → property; action/args/expensive → method). `ArgumentOutOfRangeException` + `nameof(value)` over bare `ArgumentException`. Named constants over magic numbers. Built & refactored `Product`.
- 2026-07-19 — Phase 2: **Constructors in depth** — chaining with `: this(...)` (chained ctor runs FIRST, then the body) for DRY construction; **writing any ctor removes the free parameterless one** (so `new Product { ... }` won't compile — an object initializer is shorthand for `new Product() { ... }` and still needs a ctor to call); object initializers run **after** the ctor and go **through the setters**, so accessor-level validation still protects them. `init` = "settable at creation, immutable after" — the sweet spot for identity. Forward pointer: `required` (Phase 4) exists to close the "initializer lets but doesn't force" gap. **Edge case mastered:** if a ctor throws, `new` never returns a reference → the half-built object is unreachable & GC'd; construction is all-or-nothing. Caveat: **don't let `this` escape a constructor.**
- 2026-07-19 — Phase 2: **`static`** — member belongs to the *type*, one copy for the process lifetime. Static field vs the cleaner `public static int X { get; private set; }` (world reads, class writes). Statics are shared across the **whole hierarchy** (a `DigitalProduct` increments `Product`'s counter via `base`). Danger of mutable static state: global state, hidden dependency, test leakage, **not thread-safe**. Walked the lost-update race step-by-step (`count++` = READ→ADD→WRITE; two threads both read 5, both write 6, one increment silently lost). Fixes named for Phase 4: `Interlocked.Increment`, `lock`.
- 2026-07-19 — Phase 2: **Inheritance** — `:` syntax; **constructors are not inherited**, derived must call `: base(...)` (base runs first). `virtual` = "here's a default, replace if you want" (optional override); `abstract` = "no body, you MUST implement" (abstract class only, can't be instantiated); `override` = the act of replacing. **C# is non-virtual by default** (deliberate: allowing override is a contract) — contrast with Java. `base.X` to extend rather than replace. `sealed` stops the chain. **`override` vs `new`:** override = real polymorphism (actual object type decides); `new` merely *hides* (declared reference type decides) → baffling bugs. Over-use warning: inherit only for genuine "is-a" + polymorphism, not code reuse (fragile base class). Built `DigitalProduct : Product` with `public override ShippingCost`.
- 2026-07-19 — Phase 2: **Abstract classes vs interfaces** (concept delivered; task pending) — abstract class = "is-a" + shared state/implementation/ctors; interface = "can-do", pure capability contract, no state, members implicitly `public`. Decision criteria: single-inheritance constraint (one base, many interfaces), need to share state?, are implementers related?, versioning cost (adding to an interface breaks every implementer). Heuristic: **"is-a" → abstract class, "can-do" → interface**; when both fit, prefer the interface (keeps the inheritance slot free, enables DI/testing → "program to an interface"). Default interface members noted as a versioning escape hatch, not a reason to stop thinking in is-a/can-do.
- 2026-07-19 — Phase 2: **Polymorphism, casting, `is`/`as`** — every reference has **two types**: the **compile-time (declared) type decides what you may call**, the **runtime type decides which implementation runs** (virtual dispatch). Upcast (derived→base) implicit & always safe; downcast explicit & can throw `InvalidCastException`. `is` with a **type pattern** (`if (p is DigitalProduct dp)`) is the modern default — test+cast+scope in one; `as` returns `null` instead of throwing; raw `(Derived)x` only when the type is provable. Type patterns compose in `switch`. **Design smell noted:** heavy `is`/downcasting usually means behaviour that belongs inside the class as a `virtual` member (`ShippingCost` = the good pattern; `DownloadUrl` = legitimate `is` use). Implemented a polymorphic `Product[]` loop + `IDiscountable` on `Product`.
- 2026-07-19 — Phase 2: **DRILL — `PaymentMethod` hierarchy** (self-requested extra practice; built from a blank file). `abstract class PaymentMethod` (bodyless `abstract ProcessPayment()`, `virtual TransactionFee` default 2%), `CreditCardPayment : PaymentMethod, IRefundable` (inherits the fee default), `CashPayment : PaymentMethod` (overrides fee to 0, deliberately NOT `IRefundable`), polymorphic `PaymentMethod[]` loop + `is IRefundable r` capability check. **All four concepts implemented correctly on the first attempt.** Learned: **Template Method** — `Summary()` is non-virtual but calls abstract members, so base-class code dispatches down into derived implementations. Learned: base class must be listed **before** interfaces in the base list. Fresh analogy that landed: employees/roles (can't hire "an Employee"; `DoWork` abstract; `CalculateBonus` virtual; `IDrivesCompanyCar` cuts across roles).
- 2026-07-20 — Phase 2: **`object` fundamentals — `Equals` / `GetHashCode` / `ToString`.** Every type inherits them and **the defaults are usually wrong for domain types**: `ToString()` prints the type name (useless in logs), `Equals()` is **reference** equality for classes, `GetHashCode()` follows identity not data. **The contract: if `a.Equals(b)` then `a.GetHashCode() == b.GetHashCode()` — `Dictionary`/`HashSet` find by hash bucket first, then equality, so breaking it makes lookups silently fail.** Saw **`CS0659`** live (compiler warns when you override `Equals` without `GetHashCode`) and reported the code — good error-reading habit. Implemented `ToString()`, `Equals(object?)` via the `is Product other` type pattern, and `HashCode.Combine(...)` (don't hand-roll hash math). **Key gotcha learned: overriding `Equals` does NOT change `==`** — `==` is an *operator*, defaulting to reference comparison for classes unless explicitly overloaded, so `a.Equals(b)` and `a == b` can disagree (predicted this correctly). Two further defects raised for fixing: `is Product` matches **subclasses** (a `DigitalProduct` compares equal to a `Product`) → compare exact `GetType()`; and `Product` is a **mutable hash key** (public setters) → mutating after insertion makes the object unfindable in its own `HashSet`.
- 2026-07-20 — Phase 2: **records** — one-line `record` generates value `Equals`/`GetHashCode`/`ToString` **and `==`/`!=`**, plus `Deconstruct`, copy-ctor, and `with` (non-destructive mutation). Immutable by default (positional params → `init`). Built `Money` and saw the payoff **side-by-side with the `Product` class**: `money1 == money2` → **True** (value equality) while `a == b` on two identical `Product`s → **False**, `HashSet` count 2, `ToString` = bare type name. Rule locked: **records for value objects, classes for entities with identity.** Also learned: overriding `Equals` doesn't touch `==`; validating a positional record means redeclaring the property with an `init` guard **and** consuming the primary-ctor param via a field initializer (fiddly edge case — the compiler's `CS8907` caught a wiring mistake the mentor made, reinforcing "read the actual error"). Ran the program & pasted output — **first end-to-end verified run of the session.**
- 2026-07-20 — Phase 2: **Composition over inheritance** — "has-a" vs "is-a"; favour composition, inherit only for genuine is-a + polymorphism. The spoken **"is-a" test** as the fast design check ("a Customer *is an* Address" sounds absurd → compose). Built `Customer` (class/entity) composing an `Address` (record/value object); "moving house" = swap in a new `Address` via `with` on an immutable value held in a mutable slot. Entity-composes-value-object landed cleanly.
- 2026-08-02 — Phase 4: **Exceptions done right.** Guard clause vs native runtime protection (int division by zero throws `DivideByZeroException` natively; `double` division by zero does **not** throw — silently yields `Infinity`/`NaN`, so explicit guards matter for types the runtime won't protect). **Unhandled-exception mechanism** debugged live from real broken code: an unguarded call outside any `try` crashes the *entire process* immediately — the CLR walks the stack for a matching `catch`, finds none, and terminates before reaching unrelated code further down. `finally` runs on every exit path (success, caught exception, even `return`) — verified both branches by running. **Custom exceptions**: `NegativeAmountException` carrying a structured `Amount` property (not just a string message) — a caller can react to `ex.Amount` programmatically. **Exception filters (`when`)**: filtered `catch` must come before the general one or it's unreachable; caught a self-written filter condition that was structurally impossible given the throw site's invariant (dead code) and fixed it to a reachable threshold. **Catch-all is a smell** — empty `catch (Exception) {}` swallows unknown bugs and produces silent, untraceable failures, versus a loud crash with a full stack trace. Closed with a 5-question quiz — see Quiz debt above for full results (Q1/Q5 landed, Q2/Q3 needed real rebuilding, Q3 is now a 3rd-strike persistent gap).
- 2026-08-04 — Phase 4: **`IDisposable` / `using`.** Built `FileLogger` (private `readonly StreamWriter`, `append: true` as a named arg). **The GC cleans memory, not OS handles** — files/sockets/connections/locks need *deterministic* release. `using` = pure sugar for `try/finally { Dispose(); }`, which is why it survives exceptions. **Experiment run twice**: first attempt was neutralised by his own `_writer.Flush()` per log call (lesson: *when an experiment says "everything's fine", suspect your setup*); with `Flush` removed, the no-`using` run's lines were **absent from the file** — proven by identical timestamps to the previous run. Correctly predicted they're **gone forever**: the buffer is a managed `char[]`, the GC reclaims memory without flushing, and `StreamWriter` has **no finalizer** in modern .NET. Landed the say-back: *"the data was in the writer's in-memory buffer; `using` calls `Dispose`, which flushes it to the OS and releases the handle."* Then: `using` **declaration** (`using var x = ...`) vs block form; **`Dispose` must be idempotent** (`if (_disposed) return;`); **disposed-object guards** (`ObjectDisposedException.ThrowIf(_disposed, this)`) — read the `Object name:` in the exception message to prove *which* object rejected the call (`'StreamWriter'` → his guard wasn't firing; `'Phase4.Advanced.FileLogger'` → it was); **ownership** (dispose only what you created). Also: `DateTime.Now` → **`DateTime.UtcNow`** for logs, and buffering exists — don't flush every line.
- 2026-08-04 — Phase 4: **async/await core.** **`await` is not "run on another thread"** — it's "suspend, give the thread back, resume when the result arrives"; async is about *not wasting threads*. **Measured live:** three sequential `await`s = **3009ms** (concurrency zero — the classic `foreach { await ... }` bug); starting all three first and `await Task.WhenAll(...)` = **1009ms**. Rule drilled after an imprecise say-back: **calling starts the task; awaiting collects it.** **State machine:** the compiler splits the method at each `await` into a continuation; the method returns to its caller and the continuation may resume on **any** thread — observed `8`→`8` and correctly reframed as coincidence, *no thread affinity across an `await`* (also spotted that "before" was already thread 8, i.e. execution had migrated off main via earlier awaits). **`.Result`/`.Wait()` banned** — sync-over-async deadlock via `SynchronizationContext`; async all the way down. **Async exceptions travel in the `Task`, not up the stack** — observed `WaitingForActivation` at the call site, exception surfacing only at the `await`; hence `try { BoomAsync(); } catch {}` catches nothing (*the `try` block finishes before the exception even happens*), and **`async void` has no `Task` to park the exception in** → unhandled on the thread pool → uncatchable process kill. **`Task.WhenAll`:** predicted correctly that a failure does **not** cancel siblings and that it waits for all — verified `t1: RanToCompletion, t2: Faulted, t3: RanToCompletion`; `await` rethrows only the **first** exception (rest live in `.Exception` as `AggregateException`). **`CancellationToken`:** cooperative, always the last parameter, must be *passed down*, `ThrowIfCancellationRequested()` + `Task.Delay(ms, ct)`, surfaces as `OperationCanceledException` (expected control flow, not a bug). Taught him to **predict the step count by arithmetic** (2000ms budget ÷ 500ms = steps 0–3) and spotted the **timing tie at the boundary** → moved the timeout to 2200ms to make it deterministic (test-the-edges / non-flaky-test lesson).
- 2026-08-04 — Phase 4: **Concurrency — race condition demonstrated live at last.** After three sessions of *theory* on the lost-update race, he ran it: 100 tasks × 10,000 `counter++` = expected 1,000,000, got **195,938 / 120,231 / 339,342** — >65% of increments lost, three different answers, no exception, no warning. Predicted "lower" **with the mechanism** (READ→ADD→WRITE, context switch before the write) before running — a genuine articulation win on the question he's historically missed. Fixed both ways to exactly 1,000,000: **`lock`** (`Monitor.Enter/Exit`; under contention threads spin, then the OS parks them — kernel-level and expensive) and **`Interlocked.Increment`** (a single atomic CPU instruction, `lock xadd`, nothing blocks). **`Interlocked` only covers one simple op on one variable — multi-statement invariants need `lock`.** Timing measurement was rejected as noise (6ms vs 7ms over 1M ops) with a re-run at 10× iterations requested — **he declined and moved on, so the perf comparison was never actually observed.** Closed with rules only (no task): never `lock(this)`/`lock("string")`, keep the critical section tiny, no `await`/IO inside a lock, inconsistent lock ordering = deadlock, and `Concurrent*` collections — atomic per-operation but *sequences* still race (`GetOrAdd`/`TryAdd`, not `ContainsKey`-then-`Add`).
- 2026-07-20 — Phase 2: **SOLID — SRP** (+ **DIP** & **Open/Closed** previews) — "one reason to change." Extracted tax off `Product` into a `TaxCalculator` (SRP + composition); `Product.PriceWithTax` now delegates. Verified behaviour-preserving by re-deriving `1150.0000` by hand after the refactor. Then reasoned to **Dependency Inversion**: newing a dependency inside a class hardwires it (can't swap `UKTaxCalculator`, can't fake it in tests) → depend on an abstraction, inject it (full payoff = Stage B DI container). **Open/Closed** shown live in the capstone: polymorphic `HourlyRate` means adding a `Bus` = one new class, zero edits; the `switch`-on-type version he first wrote = edit two switches (OCP violation).

---

## Struggle List (Revisit)
- **🔁 RECURRENCE ×4 — interface-vs-virtual "lie" (2026-08-04): asked for a 4th time and *opted out* ("wrap the quize and move on") rather than attempting it.** Written answer delivered per the escalation rule. **This is no longer a knowledge gap, it's an avoidance pattern on a known-weak question.** Next session: ask it cold as the very first thing, no warm-up, and require a written answer before any new topic.
- **🔁 PATTERN CONFIRMED — partial fix-lists (2026-08-04):** given exactly 3 items (guard in `Log`, idempotent `Dispose`, delete dead code) he did **1 of 3** on the first pass and reported it as done; the missing guard was only exposed because the exception message said `Object name: 'StreamWriter'` instead of `'FileLogger'`. Dead commented-out code (`FileLogger.cs:15`, `Program.cs:47–58`) was left in place and had to be flagged twice. **Keep fix-lists to ≤3 items and verify every one by reading the file — do not accept "done".**
- **⚠️ NEW — answers the conclusion, skips the mechanism, then skips half the question (2026-08-04):** three times in one session he answered only the *second* half of a two-part question (the `where is the data` half of the `using` say-back, then the `async void` half) and had to be re-asked. Also stated consequences without causes: *"`async void` bypasses normal exception handling"* (true, but the mechanism — **no `Task` to park the exception in** — is what interviews probe). **Drill: make him lead with "because…".**
- **✅ WIN — articulation improving under a demanded restatement (2026-08-04):** the lost-update race was predicted *with* the correct mechanism unprompted (a question missed repeatedly in July), and the `using`/`Dispose`/flush/handle say-back landed correctly once forced through a fill-in-the-blanks template. **The blank-filling template works — reuse it.** Also asked a genuinely good engineering question unprompted ("how can I predict the exact steps?") which turned into the deterministic-arithmetic + timing-edge-case lesson.
- **🔁 RECURRENCE ×3 — interface-vs-virtual "lie" articulation (2026-08-02):** asked why a `virtual Refund()` on a base class is worse than putting it on `IRefundable`, and instead of answering, restated the question verbatim — same failure mode as 2026-07-19 and the 2026-07-26 quiz. Only landed after a forced step-by-step walkthrough anchored to `CashPayment` (does it compile? what does it return? what's misleading about that?), and even then asked the mentor to just explain the final contrast rather than deriving it himself. **This is the single most persistent gap across the whole struggle list — 3 misses on the identical question over 2 weeks.** Next occurrence: stop everything else and do a dedicated from-scratch reteach, then require a written (not spoken) restatement.
- **🔁 RECURRENCE ×2 — `new`/hiding = declared type decides (2026-08-02):** same question missed in the 2026-07-26 quiz clear, missed again today (said the derived override value would print through a base-typed variable). Corrected via guided questions rather than direct reveal; he did eventually restate both halves (new vs override) correctly unprompted. Watch for a 3rd occurrence — that would trigger the hard drill-recurrence rule.
- **Wrong terminology reached for under pressure (2026-08-02):** called an unhandled exception from an unguarded retry call inside a `catch` block a "diamond problem" (that term is multiple-inheritance/default-interface-member ambiguity, unrelated). Corrected back to the actual mechanism already taught minutes earlier (no `try` around that call → unhandled → process dies). Pattern: under quiz pressure, reaches for an impressive-sounding term instead of the one just taught — watch for this substitution reflex.
- **Mixed up which quiz question he was answering (2026-08-02):** answered Q4-shaped content while I was still finishing Q3, twice in a row, requiring re-anchoring each time. Minor — likely fatigue after a long exceptions session — but note if it recurs.
- **🔁 RECURRENCE ×N — casing for identifiers (2026-07-27):** used **PascalCase for locals** twice in one session (`SelectedProducts`, then `Count`/`Sum`/`Average`/`MyNumbers`) — he'd been *fixing* them by pattern-match without knowing the rule. When finally asked to *state* it, answered **"snakecase and camelcase" — both wrong** (C# uses no snake_case; that's his other-language habit). Corrected to the full rule and he restated it right: **locals/params → `camelCase`, types/methods/properties → `PascalCase`, private fields → `_camelCase`.** Now *said*, not just pattern-matched — but verify it holds next session. Ties to his #1 weakness: **edits code instead of articulating the rule** (dodged the "say it" ask 3× before answering).
- Naming conventions (used snake_case initially — corrected, watch for recurrence since it's a habit from another language background)
- Initially described reference-passing as "deep copy" — corrected to "pointer/address copy"; re-quizzed 2026-07-12, answered correctly (pointer copy, mutate-through vs reassign) — looking solid, spot-check occasionally
- Refactor introduced an off-by-one boundary bug: changed `< 0` to `<= 0` on autopilot when collapsing redundant switch bounds. Lesson reinforced: **test the edges (0, 15, 16, 35), not the middles.** Watch for autopilot `<`↔`<=` changes during refactors.
- Interview articulation (not understanding): gave vague answers on (a) `decimal` vs `double` — must name binary-vs-base-10 float + accumulating rounding error; (b) `&&` as a correctness/null-guard tool, not just perf. Concepts are solid; precision of wording needs work. Re-quiz phrasing later.
- **Terminology:** called the default reference-type passing "pass by reference" — corrected to **"the reference is passed by value"** (copy of the pointer). Watch for recurrence; it's the exact phrasing interviewers probe.
- **Articulation:** could not put the `bool`+`out` design flaw into words unprompted (pasted the code instead of answering). Once shown "the caller *guessed* the failure reason," it landed. The concept — a `bool` erases *why* it failed — needs to become sayable, not just recognizable.
- **Quality habit:** typo `urget` in a parameter name — reinforced that parameter names are public contract (named-arg call sites freeze them; renaming is a breaking change). Watch for typos in identifiers.
- **⚠️ `try/catch` as a band-aid (capstone):** reached for `try/catch` to silence a `CS8601` nullability warning — wrong on two counts: (a) assigning null to a `string` slot doesn't *throw*, so the catch guarded nothing & the warning stayed; (b) exception-as-control-flow for a predictable condition (empty input) is a smell. Correct fix was `Console.ReadLine() ?? ""`. **Principle to reinforce in Phase 4: exceptions are for the unexpected, not ordinary conditions.** Watch for this reflex recurring.
- **Declare-before-use reflex:** wrote `runCli = true;` with no type declaration (CS0103) — autopilot from a dynamically-typed language. Also left a dead `= 0` initializer on `average`. Minor, but watch the "assign creates the variable" habit.
- **🔁 RECURRENCE — refactor regression (2026-07-19):** swapped magic `1.15m` for `TaxRate = 0.15m` but dropped the `(1 + ...)`, so `PriceWithTax` returned *only the tax* (150 instead of 1150 on a 1000 item). Same failure mode as the Phase 1 `<`→`<=` off-by-one: **edits the shape, loses the arithmetic.** No compiler warning catches this class of bug. **Rule: after any refactor, re-derive one concrete value by hand.**
- **🔁 RECURRENCE — terminology, "override" vs "hide" (2026-07-19):** said `new` could "override... using new keyword." Corrected: **you cannot override a non-`virtual`/non-`abstract` member at all — you can only *hide* it with `new`, and hiding is not overriding.** Got the *consequence* right unprompted (polymorphism is lost), so the concept is there; the word was wrong. Same pattern as the "pass by reference" slip — **understanding is ahead of vocabulary.** Keep drilling exact words.
- **Member-kind terminology in comments (2026-07-19, twice):** labelled `Name`/`Price` (properties) under a `// methods` comment, then put a static **property** under `// fields`. Cosmetic in code, but it's the same imprecision that costs marks in interviews. This class has fields, properties, a constructor — and zero methods.
- **Access-modifier default:** wrote `class DigitalProduct : Product` with no modifier → defaults to **`internal`**, giving a `public` base an invisible subclass. Remember: top-level types default to `internal`, members default to `private`.
- **🔁 PATTERN — reports that the code *builds*, not what it *does* (2026-07-20):** on the `Equals`/`GetHashCode` exercise the whole point was to observe `HashSet.Count` flip from `2` to `1`; reported only "build success after override GetHashCode()". Same shape as the articulation gap: **treats "it compiles" as "it works".** Tie-in to the new CLAUDE.md rule — a task isn't done until the output has been seen and reported. **Always make him state the observed values, not the build status.**
- **Fix-lists get partially done (2026-07-20):** 3 of 6 review items completed, 3 silently skipped — and one skipped item (`IDiscountable` variable) had already been skipped once before. Tends to do the mechanical fixes (`protected`, `public override`, drop `$`) and skip the ones requiring a judgment call. **Re-check every outstanding item explicitly rather than assuming a fix-list was completed.**
- **🚩 BIGGEST PATTERN — cosmetic tweaks instead of the structural change (2026-07-20, capstone):** given three structural fixes for the parking orchestration, responded with renamed spot ids + a constructor arg and **none** of the three. Then, asked 3× to make `Space` a *class* with `private set` + `Occupy()`/`Free()` (exact target code supplied), re-submitted a **`record` with public setters and no methods** every time, and didn't answer the reasoning questions. **Not a knowledge gap** — he'd articulated record-vs-class perfectly an hour earlier. Read as **fatigue + overwhelm from oversized reviews late in a very long session.** Mentor course-corrected: shrink reviews to one file / one token; when engagement drops to tweak-paste-tweak, **stop and wrap, don't grind.** For next session: keep each step tiny, make him *reason aloud first*, and don't hand multi-file fix-lists.
- **Declare-before-use reflex resurfaced (2026-07-20):** `Program.cs` capstone had `while (runCli) {}` — an infinite empty loop; `runCli` declared but never mutated and the body never does anything. Same "assign/loop without wiring the exit" shape as the Phase 1 `runCli` note.
- **❌ MUST-KNOW MISSED — abstract classes cannot be instantiated (2026-07-19):** asked what `new PaymentMethod(100m)` does, answered *"creates a PaymentMethod object with amount 100"*. It is a **compile error (`CS0144`)**. Taught twice before the question (incl. the "you can't hire *an Employee*" analogy) and still missed — so it was recognition, not retention. Reinforced via the follow-on rule: **an abstract class's constructor should be `protected`, not `public`**, because only derived classes can ever reach it. **Re-quiz this specifically.**
- **🔁 RECURRENCE ×3 — `override public` instead of `public override` (2026-07-19):** flagged on `DigitalProduct`, fixed there, then reintroduced in *both* `CashPayment` and `CreditCardPayment`. **Access modifier always comes first.** The fix isn't sticking because it's being corrected per-file rather than learned as a rule.
- **Articulation (again) — stated the conclusion, not the mechanism (2026-07-19):** asked how a non-virtual `Summary()` prints `"Cash received"`, answered only *"decided at runtime."* True but empty. Needs: *"`Summary` is non-virtual, but it calls an abstract/virtual member, so that inner call is virtually dispatched to the runtime type."* **Same pattern as the `bool`+`out` and `decimal`-vs-`double` gaps: the idea is present, the explanation isn't.** This is now the single most consistent weakness.
- **Capability-on-base-class blind spot (2026-07-19):** could not explain why `IRefundable` beats a `virtual Refund()` returning 0 for cash ("no idea"). Key idea to retain: **a virtual `Refund()` on the base makes the type system lie** — `cash.Refund(10)` would compile, forcing a silent wrong answer or a runtime `NotSupportedException`, whereas the interface makes it a **compile error**. Degenerate implementations (`return 0` / `throw`) signal the member is on the wrong type. Links to Interface Segregation (SOLID, upcoming).
- **Style: ternary-with-`throw` used as a guard clause (2026-07-19):** `return amount > Amount ? throw new ... : amount;` — legal but not idiomatic; a ternary chooses between *values*. Use an early-return guard. Also the guard was incomplete (missed `amount <= 0`), echoing the Phase 1 "test the edges" lesson.
- **⚠️ Validate at the accessor, not the constructor (2026-07-19):** guarded `downloadUrl` in the ctor while leaving `{ get; init; }` unguarded → an object initializer can overwrite it with whitespace *after* the ctor and bypass validation entirely. **The accessor is the chokepoint every write path must cross.** He'd already done this right for `Name`/`Price`; didn't transfer the pattern to `init`. Re-check on next validation task.

---

## Pending Tasks
- **🔴 FIRST THING NEXT SESSION — ask the interface-vs-virtual question cold** (4 misses/skips, see Struggle List). Written answer required before any new material.
- **Unfinished from 2026-08-04:** (a) the `lock` vs `Interlocked` **perf comparison was never validly measured** — re-run at 100,000 iterations per task and observe which wins and by how much; (b) **delete the dead commented-out code** in `Phase4.Advanced/Program.cs` (the old logger blocks) and `FileLogger.cs:15`.
- **Phase 4 quiz debt (2026-08-02):** re-quiz Q2 (`new`/hiding, 2nd miss) and especially Q3 (interface-vs-virtual "lie", 3rd miss — see Struggle List) next session before moving on much further. Run Q4 (catch-filter ordering) — never asked. Do this early in the next session while it's fresh, not deferred indefinitely like the Phase-2 quiz debt was.
- **Phase 4, next topic: `IDisposable` / `using`** — deterministic cleanup, what happens when you forget to dispose, `using` statement vs `using` declaration.
- **Repair the .NET SDK** (see the environment warning at the top) — until then nothing can be run or verified, only read.
- **Outstanding fix-list (3 of 6 done as of 2026-07-20):**
  - ✅ `PaymentMethod` ctor → `protected`; ✅ `public override` in both payment classes; ✅ dropped the pointless `$`.
  - ❌ `Program.cs`: call `GetDiscountedPrice` **through an `IDiscountable` variable** (`IDiscountable d = product;`) — **skipped twice now**; also pass `10m` not `10`.
  - ❌ `CreditCardPayment.Refund` → guard-clause instead of ternary-with-`throw`, plus the missing `amount <= 0` check.
  - ❌ Spacing: `public class Product : IDiscountable`.
- **New from the `Equals` review (2026-07-20):**
  - Fix `Equals` to compare **exact runtime type** (`GetType()`) instead of `is Product` — currently a `DigitalProduct` can compare equal to a `Product`.
  - Tidy the self-contradicting comment on `Program.cs` line ~71 (`a == b` comment says "also be false" right after the `Equals` comment says true).
  - **Report observed values (never delivered):** `HashSet.Count` **with** and **without** `GetHashCode`; and the mutable-key demo — `set.Contains(a)` before vs after `a.Price = 999m`.
- **Run the whole Phase 2 program locally and paste the output** — build is verified (0 warnings), runtime behaviour still unobserved. Expected payments section: `Charged card | fee: 2.00` / `Refund Amount: 30.00` / `Cash Received | fee: 0.00` (no refund line for cash). *(The `CS8618` I flagged on `_downloadUrl` did NOT occur — the compiler traces assignment through an `init` accessor.)*
- **Quiz debt — CLEARED 2026-07-26** (5-question quiz run). Results: Q1 abstract-class instantiation ✅ (said compile error, correct); Q2 lost-update race — conclusion right (6) but **mechanism wrong** ("one sees fresh data and skips" — corrected to *read-modify-write not atomic, both read stale 5*); Q3 `new`/hiding ❌ (said derived runs — corrected: hiding → **declared type** decides, base runs); Q4 interface-vs-virtual ❌ (restated question — corrected: base `virtual` makes the type system *lie*, cash.Refund compiles & returns 0 silently → runtime bug; interface = compile error); Q5 `bool`+`out` — couldn't answer (given: bool collapses many failure reasons into one bit, destroys the *why*). **Net: concepts present, mechanisms still not sayable — the persistent top weakness. Not a Phase-3 blocker; re-drill Q2/Q3/Q4 phrasing.** Needed the "two types" idea re-taught from scratch (declared/compile-time vs runtime) and Example-2 (interface capability tag) re-explained with a concrete cash-refund scenario before it landed.

---

## Session Log
| Date       | Topics Covered                          |
|------------|------------------------------------------|
| 2026-08-04 | **Phase 4: `IDisposable` + the async core + concurrency.** Quiz debt closed by *written delivery* (he opted out of answering — interface-vs-virtual now 4 misses, escalated). **`IDisposable`:** built `FileLogger`, proved unflushed buffer data is lost forever (no finalizer on `StreamWriter`), `using` = `try/finally{Dispose()}`, idempotent `Dispose`, `ObjectDisposedException.ThrowIf` guards, ownership, `UtcNow` for logs. Read `Object name:` out of an exception to prove whose guard fired. **async/await:** 3009ms → 1009ms via `Task.WhenAll` ("calling starts the task, awaiting collects it"), state machine + continuations + no thread affinity across `await`, `.Result`/`.Wait()` banned (sync-over-async deadlock), async exceptions live **in the `Task`** (`WaitingForActivation` observed) → `async void` has nowhere to park them, `WhenAll` doesn't cancel siblings & rethrows only the first, `CancellationToken` (cooperative, last param, pass it down) with a **timing tie at the 2000ms boundary** found and removed. **Concurrency:** the lost-update race finally *observed* — 1,000,000 expected, **195,938 / 120,231 / 339,342** actual; predicted "lower" **with the correct mechanism**; fixed with `lock` and `Interlocked` (both exact), perf comparison rejected as measurement noise and **not re-run**. Closed with lock pitfalls + `Concurrent*` collections as rules. **Next: memory & the GC.** |
| 2026-08-02 | **Phase 4 started.** Created `Phase4.Advanced`, added to solution. **Exceptions done right**: guards vs native protection (`int` throws natively, `double` silently produces `Infinity`), unhandled-exception-kills-the-process debugged live from his own broken code (stray call outside `try`), `finally` on both exit paths, custom `NegativeAmountException` with structured `Amount`, exception filters (`when`) incl. catching a self-authored dead-code filter, catch-all-is-a-smell. **5-question quiz**: Q1 (lost-update race) finally landed cleanly with correct READ/MODIFY/WRITE vocabulary; Q2 (`new`/hiding) 2nd miss, corrected; **Q3 (virtual-base-lies vs interface, 3rd miss — persistent gap, flagged for dedicated reteach if missed again)**; Q4 not reached; Q5 corrected from vague to the real loud-vs-silent-failure mechanism. Also corrected a wrong-term reach ("diamond problem" for an unrelated concept). **Next: `IDisposable`/`using`.** |
| 2026-07-10 | Phase 0 complete: SDK/runtime, dotnet CLI, IL/CLR/JIT, .csproj/.slnx anatomy. `CSharpMastery` solution created with `Phase1.Foundations` project. |
| 2026-07-11 | Phase 1 started: top-level statements, namespaces, using, entry-point rule, assemblies vs repos, access modifiers, static vs instance, naming conventions. Built/reviewed `Greeter` class through 2 PR-review rounds. |
| 2026-07-12 | Phase 1: operators (`&&`/`&` short-circuit), `switch` expressions + relational/`and`/`or` patterns, expression-bodied methods. Built `DescribeTemperature` (3 PR rounds — collapsed redundant bounds, caught off-by-one at `0`, converted to `=>` body). 5-question spaced-rep quiz: 3 solid, 2 needed articulation polish (`decimal` vs `double`, `&&` correctness). |
| 2026-07-14 | Phase 1: **Methods** — pass-by-value default (reference-passed-by-value vs true `ref`), `ref`/`out`/`in`, optional/named args, overloading, `bool`+`out` design trade-off. Built `TryDivide`, `Swap`, `Send`, `Describe` overloads — all correct & running. Corrected "pass by reference" terminology; flagged `urget` param typo (public contract). Pending: fix typo, cover `out`-failure articulation in a later quiz. |
| 2026-07-16 | Phase 1: **Arrays** (fixed-size ref type, default-init trap, bounds, int-division cast) + `Span<T>` seed. **CAPSTONE: Expense Tracker CLI** built & verified end-to-end over 3 build rounds. Key teachable: `try/catch` misused to silence a nullability warning → corrected to `?? ""` (exception-as-control-flow smell). **Phase 1 COMPLETE — Phase 2 (OOP) is next.** |
| 2026-07-19 | **Phase 2 started** (`Phase2.ObjectOriented`). Covered: properties & encapsulation (backing fields, `init`, computed, validation in accessors, ctor-through-setters), constructors in depth (`: this(...)` chaining, vanishing default ctor, object initializers, ctor-throws/half-built-object), `static` (+ the `count++` lost-update race walked step by step), inheritance (`: base(...)`, `virtual`/`abstract`/`override`, `override` vs `new` hiding, fragile base class), abstract vs interface decision criteria. Built `Product` (5 review rounds) and `DigitalProduct`. **Caught: `PriceWithTax` refactor regression, property-vs-method mix-up, missing `public` on a type, and an `init`-accessor validation bypass.** ⚠️ SDK corrupted — all review done by reading; nothing executed. Task on abstract/interface issued, not yet submitted. |
| 2026-07-19 | (same session, cont.) **Polymorphism, casting, `is`/`as`** — compile-time vs runtime type, virtual dispatch, type patterns. Then, **at his own request for more reps on `virtual`/`override`/`abstract`/interface**, built the `PaymentMethod` drill from a blank file: abstract base + abstract & virtual members, `CreditCardPayment : PaymentMethod, IRefundable`, `CashPayment` (not refundable), polymorphic array + `is IRefundable` capability check. **All four concepts correct first try** — the structure is genuinely understood. Gaps were polish (`protected` ctor, `override public` ×3 recurrence, skipped the `IDiscountable`-variable step, ternary-`throw` guard) plus **two conceptual misses: abstract-class instantiation (`CS0144`) and why a capability belongs on an interface rather than a `virtual` on the base.** Articulation remains the top weakness — states conclusions, not mechanisms. |
| 2026-07-20 | **Environment fixed** (snap finished refreshing to SDK 10.0.110 / runtime 10.0.10 — build now clean, 0 warnings). **`Equals`/`GetHashCode`/`ToString`**: implemented all three on `Product`, hit `CS0659` live and read the error properly, learned the hash/equality contract and that **overriding `Equals` doesn't change `==`**. Raised two latent defects (subclass-matching `Equals`, mutable hash key). **`CLAUDE.md` rewritten** with a learner profile (curious junior, **learn-by-doing**, thin/uneven knowledge), a doing-biased teaching pattern, four new hard rules, an **Engineering Practices** section (git, debugging/error-reading, code quality, testing mindset, professional habits) and a **Security** section (mindset in Stage A, secure-by-default endpoints in Stage B). Fix-list only 3/6 done; observed values still not reported. |
| 2026-07-20 | (same long session, cont.) **records** (built `Money`, saw value-vs-reference semantics side-by-side with `Product`, **first verified end-to-end run**), **composition over inheritance** (`Customer` composes `Address`), **SOLID / SRP** (extracted `TaxCalculator`, re-derived `1150.0000` to prove the refactor safe; reasoned to **DIP**; **Open/Closed** shown live). Started the **Phase 2 capstone — Parking Lot (`Phase2.FinalTask`)**: designed on paper, built the **vehicle layer correctly** (abstract `HourlyRate`/`RequiredSize`, real polymorphism, after correcting a type-switch anti-pattern). **Orchestration layer incomplete** — `Space` (still a record, no guarded invariant), `ParkingLot` (doesn't compose spots → 3/4 invariants unenforced), and `Program` (empty infinite loop) all need work. Session ended on **fatigue/overwhelm** — engagement dropped to cosmetic tweaks; wrapped rather than grind. **`CLAUDE.md` rewrite done earlier this session** (learner profile, engineering practices, security). Resume at the "🅿️ Capstone status" block. |
| 2026-07-27 | **Phase 2 CAPSTONE COMPLETED & VERIFIED.** Finished the Parking Lot orchestration in tiny single-file steps (deliberately, after last session's overwhelm spiral). `Spots` record → **`Space` class**: took **~5 rounds** to land — repeatedly resubmitted as record / kept a public setter / added an **inverted `Size` guard that threw on every valid size** (caught by re-deriving by hand — the recurring "edits shape, loses logic" pattern) — but got there: `init` identity, `{ get; private set; }`, guarded `Occupy()`/`Free()` with `InvalidOperationException`. `ParkingLot` now **composes `List<Space>`**, `Park` = first-fit `Size >= RequiredSize` + occupy + throw-if-none, dropped redundant `_vehicles`; `ExitVehicle` frees the space via LINQ `.First()`. `ParkingData` → **`DateTime? ExitTime`** (sentinel + `Math.Max` band-aid removed). **Ran locally (snap now runs!):** motorcycle→A1, car→B1, truck→C1, exit fee **$10.00** (5h×$2), 4th truck → "no spot" throw. Then **quiz-debt cleared** (see Pending — 2/5 solid, mechanisms still weak). **User feedback mid-session: "your replies are too much detailed"** → switched to terse numbered fix-lists (saved as a memory). **Phase 2 COMPLETE.** |
| 2026-07-28 | **Phase 3 COMPLETED.** **CSV capstone** (`Phase3.PowerTools`, `sales.csv`): modelled rows as a `Sale` record, `SalesLoader.LoadSales` (`ReadAllLines`→`Skip(1)`→`Select` parse→`ToList`); debugged a live **`FileNotFoundException`** — relative path resolves against the *working directory*, not the code/exe location; fixed with `Path.Combine(AppContext.BaseDirectory, ...)` + `CopyToOutputDirectory` in csproj (real engineering lesson). Then **8 LINQ queries** (Sum revenue, MaxBy, Where+OrderBy, GroupBy revenue/avg per category, revenue>3000, named-tuple `GetSummary` deconstructed). **Two "looks-right-but" catches:** a Price/Quantity logic bug that returned the right answer by luck, and a hidden **count tie** (both categories = 5) that `MaxBy` silently masked. Casing was correct throughout — the rule stuck. **Events:** asked for a proper intro (got publisher/subscriber/handler/raise vocab + loose-coupling motivation + newsletter analogy); built `BankAccount` with `event EventHandler<decimal>? Overdrawn` raised via `?.Invoke(this, amount)`, subscribed in `Program`; learned the `sender`/args convention and handler-signature-must-match rule. **Event memory leak** taught + correct say-back with the *mechanism* (publisher holds a reference → subscriber stays reachable → GC can't collect; fix = symmetric `-=`, named handlers, watch `static` events). **Phase 3 done → Phase 4 next.** |
| 2026-07-27 | **Phase 3 continued (big session).** **Delegates/lambdas** (`Action`/`Func`/`Predicate`, higher-order `ApplyToEach`, **closure captured-variable trap** — predicted `3 3 3` *with* the mechanism, another articulation win; `for` shares `i`, `foreach` per-iteration, fix = local copy). **LINQ deep:** `Where`/`Select`/`OrderByDescending`/`First`-`FirstOrDefault`-`Single` (built the semantics table, `Single` throws on >1), `GroupBy`+`Average` with anonymous-type projection over a `Product` record list. **Extension methods:** built his own `MyWhere<T>` — proved LINQ = generics+extension+delegate+`yield`, and that its laziness is *deferred* because of `yield`. **Tuples & deconstruction:** `GetStats` named tuple → `var (count, sum, average)`. **Recurring bugs caught:** `WriteLine(list)` prints the type name not values (comment lied about output — the "reports it works without running" habit); **PascalCase-for-locals ×2** → drilled the casing rule until he *stated* it (first tried "snakecase and camelcase", both wrong). All code ran & verified. **Only the CSV capstone remains in Phase 3.** |
| 2026-07-27 | (earlier same day) **Phase 3 started** — created `Phase3.PowerTools`. **Generics:** motivated via `object`+boxing pain; built generic class `Box<T>` (`default!` field) and generic method `MathUtils.Max<T> where T : IComparable<T>`; taught type inference (`Max(10,20)` not `Max<int>`), static-class-for-utilities. **Collections:** List/Dictionary/HashSet chosen by access pattern (anchored to the capstone's own choices); built `TextUtils.CountWords` → Dictionary, upgraded `ContainsKey`/if-else → `GetValueOrDefault(word)+1` one-liner (caught a regression where he'd changed it to a void method mutating a passed-in dict — reverted to pure return). **Iterators:** `yield return`, deferred execution **proven** (print-order prediction — he gave the *mechanism*, a win for his articulation weakness), then the **multiple-enumeration trap** live (2 foreach = "-- Generating --" twice; real-world = double DB hit) fixed with `.ToList()`. All code ran & verified. **Next: delegates/lambdas → events → LINQ deep.** |
